-- Udhari Khata Seed SQL File
-- Note: Replace '<USER_UUID_FROM_AUTH_USERS>' with the actual UUID generated when creating the initial shop owner account in Supabase Auth.

-- Example profile update for initial shop owner:
-- UPDATE public.profiles
-- SET role = 'owner', display_name = 'मालक (Dairy Owner)'
-- WHERE id = '<USER_UUID_FROM_AUTH_USERS>';

-- Instructions:
-- 1. Register the first user via the app login/signup screen or Supabase Auth Dashboard.
-- 2. Run the SQL query above in Supabase SQL Editor to assign the 'owner' role.
